:root { color-scheme: dark; }
* { box-sizing: border-box; }

body {
  margin: 0;
  background: #0b0d11;
  color: #e8eaed;
  overflow: hidden;
  font: 15px/1.5 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  cursor: crosshair;
}

canvas { display: block; }
[hidden] { display: none !important; }

.dim { color: #9aa0a6; font-size: 13px; }

#hud { position: fixed; left: 16px; bottom: 16px; }
#hud b { font-size: 30px; font-variant-numeric: tabular-nums; display: block; }
#hold { display: block; color: #fdd663; font-size: 13px; margin-top: 2px; }
#perk { display: block; font-size: 13px; margin-top: 2px; }
#perk.harpoon { color: #8ab4f8; }
#perk.blast { color: #ff8bcb; }
kbd { font: inherit; border: 1px solid #3c4043; border-bottom-width: 2px; padding: 0 4px; border-radius: 3px; }

#board {
  position: fixed; top: 14px; right: 16px; width: 190px;
  background: #12151add; padding: 10px 12px;
}
#board h2 { font-size: 11px; margin: 0 0 7px; color: #9aa0a6; text-transform: uppercase; letter-spacing: .07em; }
#board ol { margin: 0 0 6px; padding: 0; list-style: none; }
#board li { display: flex; justify-content: space-between; gap: 8px; font-variant-numeric: tabular-nums; }
#board li.me { color: #8ab4f8; }

#gate, #over, #won { position: fixed; inset: 0; display: grid; place-items: center; background: #0b0d11; }
#over, #won { background: #0b0d11cc; }
#gate div, #over div, #won div { text-align: center; max-width: 25rem; padding: 0 1rem; }
#champion { color: #fdd663; }
h1 { margin: 0 0 .4rem; }

input { padding: 9px 11px; border: 1px solid #3c4043; background: #12151a; color: inherit; font: inherit; }
button { padding: 9px 20px; border: 0; background: #1a73e8; color: #fff; font: inherit; cursor: pointer; }

/* The teleport, which everybody has, and the one pickup that is drawn rather
   than thrown. Kept to the same shape as the rest of the hud so the row reads
   as one line of state and not as three separate notices. */
#warp {
  display: block;
  font-size: 13px;
  margin-top: 2px;
  font-variant-numeric: tabular-nums;
}

#spark {
  display: block;
  font-size: 13px;
  margin-top: 2px;
  font-weight: 600;
  color: #8adcff;
  text-shadow: 0 0 14px rgba(138, 220, 255, .6);
}

#warp.ready { color: #8adcff; }
#warp.cooling { color: #7c8698; }
#warp.collapsed { color: #c884ff; font-weight: 600; }

#perk.streak { color: #8adcff; }

/*
 * What just happened, in the middle of the screen and briefly.
 *
 * Above the blob rather than below it: a kill is read out of the corner of
 * an eye that is on its own blob, and anything down by the board is missed
 * by the person it is about. It leaves on its own - there is nothing here to
 * click, and a message that waits to be dismissed is in the way.
 */
#feed {
  position: fixed;
  left: 50%;
  top: 17%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  pointer-events: none;
  z-index: 5;
}

.said {
  font-size: 22px;
  font-weight: 600;
  letter-spacing: .2px;
  color: #fff;
  text-shadow: 0 2px 14px rgba(0, 0, 0, .9), 0 0 26px rgba(138, 220, 255, .45);
  animation: said 2.4s cubic-bezier(.16, 1, .3, 1) forwards;
  white-space: nowrap;
}

@keyframes said {
  0%   { opacity: 0; transform: translateY(14px) scale(.86); }
  10%  { opacity: 1; transform: none; }
  70%  { opacity: 1; transform: none; }
  100% { opacity: 0; transform: translateY(-18px); }
}

#crowd {
  position: fixed;
  right: 18px;
  bottom: 18px;
  padding: 8px 14px;
  border: 1px solid rgba(255, 255, 255, .16);
  border-radius: 999px;
  background: rgba(14, 18, 26, .82);
  color: #c3cbd9;
  font: inherit;
  font-size: 13px;
  cursor: pointer;
  backdrop-filter: blur(6px);
}

#crowd:hover { color: #fff; border-color: rgba(255, 255, 255, .34); }
