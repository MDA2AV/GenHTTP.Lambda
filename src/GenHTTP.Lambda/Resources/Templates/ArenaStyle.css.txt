:root { color-scheme: dark; }
* { box-sizing: border-box; }

body {
  margin: 0;
  background: #0b0d11;
  color: #e8eaed;
  overflow: hidden;
  overscroll-behavior: none;
  font: 15px/1.5 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  cursor: crosshair;
  -webkit-user-select: none;
  user-select: none;
  -webkit-tap-highlight-color: transparent;
}

/*
 * No browser gestures on the playing surface.
 *
 * Without this a drag scrolls the page, a second finger zooms it, and a long
 * press brings up a menu over the arena - all three of which happen during
 * ordinary play and none of which can be undone once they have started.
 */
canvas { display: block; touch-action: none; }
[hidden] { display: none !important; }

.dim { color: #9aa0a6; font-size: 13px; }

/*
 * Everything that only reports goes transparent to the pointer.
 *
 * A panel with a bounding box over the arena is a patch of floor you cannot
 * steer to, cannot draw a line through and, for a hole, cannot open a whirl
 * on - and it is not obvious that is what is happening, it just feels like
 * the game stopped listening in one corner.
 */
#hud, #rail, #chat, #feed { pointer-events: none; }
#chatbox { pointer-events: auto; }

#hud { position: fixed; left: 16px; bottom: 16px; }
#hud b { font-size: 30px; font-variant-numeric: tabular-nums; display: block; }
#hold { display: block; color: #fdd663; font-size: 13px; margin-top: 2px; }
#perk { display: block; font-size: 13px; margin-top: 2px; }
kbd { font: inherit; border: 1px solid #3c4043; border-bottom-width: 2px; padding: 0 4px; border-radius: 3px; }

#board {
  width: 190px;
  background: #12151add; padding: 10px 12px;
  border-radius: 8px;
}
#board h2 { font-size: 11px; margin: 0 0 7px; color: #9aa0a6; text-transform: uppercase; letter-spacing: .07em; }
#board ol { margin: 0 0 6px; padding: 0; list-style: none; }
#board li { display: flex; justify-content: space-between; gap: 8px; font-variant-numeric: tabular-nums; }
#board li.me { color: #8ab4f8; }

#gate, #over, #won { position: fixed; inset: 0; display: grid; place-items: center; background: #0b0d11; }
#over, #won { background: #0b0d11cc; }
#gate div, #over div, #won div { text-align: center; max-width: 25rem; padding: 0 1rem; }
#champion { color: #fdd663; }
h1 { margin: 0 0 .4rem; }

input { padding: 9px 11px; border: 1px solid #3c4043; background: #12151a; color: inherit; font: inherit; }
button { padding: 9px 20px; border: 0; background: #1a73e8; color: #fff; font: inherit; cursor: pointer; }

/* The teleport, which everybody has, and the one pickup that is drawn rather
   than thrown. Kept to the same shape as the rest of the hud so the row reads
   as one line of state and not as three separate notices. */
#warp {
  display: block;
  font-size: 13px;
  margin-top: 2px;
  font-variant-numeric: tabular-nums;
}

#spark {
  display: block;
  font-size: 13px;
  margin-top: 2px;
  font-weight: 600;
  color: #8adcff;
  text-shadow: 0 0 14px rgba(138, 220, 255, .6);
}

#warp.ready { color: #8adcff; }
#warp.cooling { color: #7c8698; }
#warp.collapsed { color: #c884ff; font-weight: 600; }

#perk.streak { color: #8adcff; }

/*
 * What just happened, in the middle of the screen and briefly.
 *
 * Above the blob rather than below it: a kill is read out of the corner of
 * an eye that is on its own blob, and anything down by the board is missed
 * by the person it is about. It leaves on its own - there is nothing here to
 * click, and a message that waits to be dismissed is in the way.
 */
#feed {
  position: fixed;
  left: 50%;
  top: 17%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  pointer-events: none;
  z-index: 5;
}

.said {
  font-size: 22px;
  font-weight: 600;
  letter-spacing: .2px;
  color: #fff;
  text-shadow: 0 2px 14px rgba(0, 0, 0, .9), 0 0 26px rgba(138, 220, 255, .45);
  animation: said 2.4s cubic-bezier(.16, 1, .3, 1) forwards;
  white-space: nowrap;
}

@keyframes said {
  0%   { opacity: 0; transform: translateY(14px) scale(.86); }
  10%  { opacity: 1; transform: none; }
  70%  { opacity: 1; transform: none; }
  100% { opacity: 0; transform: translateY(-18px); }
}

/*
 * Thumb controls. Bottom right, where a hand already is, and large enough to
 * hit without looking: everything else on this screen is being looked at.
 */
/*
 * The minimap, under the board because they are the same kind of thing: both
 * are about the arena rather than about what is in front of you.
 */
#rail {
  position: fixed;
  right: 16px;
  top: 16px;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 10px;
  z-index: 4;
}

#map {
  width: 160px;
  height: 160px;
  border: 1px solid rgba(255, 255, 255, .16);
  border-radius: 8px;
  background: rgba(8, 10, 16, .72);
  pointer-events: none;
  z-index: 4;
}

/*
 * Chat, on the left above the score. The log is always there and always
 * fading; the box is only there while somebody is typing, because a text
 * field permanently on screen is a text field permanently catching keys that
 * were meant for the game.
 */
#chat {
  position: fixed;
  left: 16px;
  bottom: 122px;
  width: min(360px, 42vw);
  display: flex;
  flex-direction: column;
  gap: 6px;
  z-index: 5;
}

#chatlog {
  display: flex;
  flex-direction: column;
  gap: 3px;
  max-height: 168px;
  overflow: hidden;
  pointer-events: none;
}

#chatlog div {
  font-size: 13px;
  line-height: 1.35;
  color: #e8eaed;
  text-shadow: 0 1px 6px rgba(0, 0, 0, .95);
  word-break: break-word;
  animation: fades 24s linear forwards;
}

#chatlog b { color: #8adcff; font-weight: 600; }
#chatlog .mine b { color: #fdd663; }

@keyframes fades {
  0%, 70% { opacity: 1; }
  100% { opacity: 0; }
}

#chatbox {
  padding: 8px 12px;
  font: 14px/1.2 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  color: #e8eaed;
  background: rgba(10, 13, 20, .92);
  border: 1px solid rgba(138, 220, 255, .5);
  border-radius: 8px;
  outline: none;
}

#thumbs {
  position: fixed;
  right: 16px;
  bottom: 16px;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 12px;
  z-index: 6;
}

#thumbs button {
  min-width: 96px;
  padding: 18px 20px;
  font: 600 15px/1 -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  color: #e8eaed;
  background: rgba(16, 20, 30, .82);
  border: 1px solid rgba(255, 255, 255, .22);
  border-radius: 999px;
  touch-action: none;
}

#thumbs button.on {
  color: #0b0d11;
  background: #8adcff;
  border-color: #8adcff;
}

#spend { color: #8adcff; border-color: rgba(138, 220, 255, .5) !important; }

/*
 * The bot button, out of the way of two things at once.
 *
 * It sat bottom right, which is where the thumbs now are, and moving it to
 * the bottom left put it straight on top of the score. Top left is the only
 * corner a phone has left - the board has the other one - and it gets a
 * bigger target while it is there, since it is being hit with a thumb.
 */
body.touch #crowd {
  right: auto;
  bottom: auto;
  left: 16px;
  top: 16px;
  padding: 14px 18px;
}

/*
 * A phone is not a small desktop. The board is the thing that has to give -
 * it is the only element that is both large and not needed to play.
 */
@media (max-width: 620px) {
  #map { width: 116px; height: 116px; }
  #chat { width: min(300px, 62vw); bottom: 108px; }
  #chatlog { max-height: 108px; }

  #board { padding: 10px 12px; font-size: 13px; }
  #board h2 { font-size: 11px; }
  #board li { font-size: 12px; }
  #hud b { font-size: 24px; }
  #hint { display: none; }
  #gate > div, #over > div, #won > div { padding: 20px; max-height: 88vh; overflow-y: auto; }
}

#crowd {
  position: fixed;
  right: 18px;
  bottom: 18px;
  padding: 8px 14px;
  border: 1px solid rgba(255, 255, 255, .16);
  border-radius: 999px;
  background: rgba(14, 18, 26, .82);
  color: #c3cbd9;
  font: inherit;
  font-size: 13px;
  cursor: pointer;
  backdrop-filter: blur(6px);
}

#crowd:hover { color: #fff; border-color: rgba(255, 255, 255, .34); }
