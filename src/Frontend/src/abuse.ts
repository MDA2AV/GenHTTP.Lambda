/**
 * Where a report goes.
 *
 * One constant rather than the address written into each of the three places
 * that mention it, so a mailbox that moves does not leave a dead link behind
 * on the page nobody thought to check.
 */
export const ABUSE_MAILBOX = 'abuse@genhttp.dev';
